% import water depth data
irreg = load('earthsurface_x_y-2.mat');
x_irreg = irreg.x_es; % irregularly sampled x-position [km]
y_irreg = irreg.y_es; % irregularly sampled y-position [km]

% load initial wave height at the moment of rupture on irregular grid
h_irreg = load('tsunami.mat');  
h_irreg_load = h_irreg.tsunami;

% get rid of overlapping locations in the x-position
h_irreg_load(diff(x_irreg)==0) = [];
y_irreg(diff(x_irreg)==0) = [];
x_irreg(diff(x_irreg)==0) = [];

% interpolation on regular grid
x = xmin:dx:xmax;
nx = length(x);
y = -interp1(x_irreg,y_irreg,x'); % sea floor depth [km]
h_reg = interp1(x_irreg,h_irreg_load,x'); % wave height

%{
% import water depth data
irreg = load('nankai');
x_irreg = irreg.x_sf + 8127; % irregularly sampled x-position starting from 0[km]
y_irreg = irreg.y_sf; % irregularly sampled y-position [km]

% initial wave height at the moment of rupture on irregular grid
x_OS = irreg.x_OS + 8127;
h_irreg = irreg.uyOS_ng;

% get rid of overlapping locations in the x-position
h_irreg(diff(x_OS)==0) = [];
x_OS(diff(x_OS)==0) = [];
y_irreg(diff(x_irreg)==0) = [];
x_irreg(diff(x_irreg)==0) = [];

% interpolation on regular grid
x = xmin:dx:xmax;
nx = length(x);
y = -interp1(x_irreg,y_irreg,x'); % sea floor depth [km]
h_reg = interp1(x_OS,h_irreg,x'); % wave height